{
  "name": "@sting8k/pi-vcc",
  "version": "0.3.12",
  "description": "Algorithmic conversation compactor for pi - transcript-preserving structured summaries, no LLM calls",
  "main": "index.ts",
  "keywords": [
    "pi-package",
    "pi-extension",
    "vcc",
    "compact",
    "compaction"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sting8k/pi-vcc.git"
  },
  "scripts": {
    "test": "vitest run",
    "syntax-check": "node $(git rev-parse --show-toplevel)/.mise/plugins/deploy/scripts/compile-extensions.js --check-only --single .",
    "prepublishOnly": "npm run syntax-check"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@sinclair/typebox": "*"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
